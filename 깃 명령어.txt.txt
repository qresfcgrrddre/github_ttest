- git 저장소 만드는 명령어: git init
- 현재 폴더안에 있는 파일 확인: ls
- 깃에 추적기 달기:git add 파일이름
- git status: 
- clear: 
- git commit -m "첫번째 버전"
- git config --global user.name "사용자 이름"
- git config --global user.email "사용자이메일"
- git commit -am "두번째 버전" : 이미 추적중인것은 따로 조치를 안취해도 됨
- git checkout 해시값 : 이전 상태로 돌아갈 수 있다.
- git checkout master: 마지막에 작업했던 상태로 돌리기
- git branch -M 원하는 작업자 이름 :  작업자 이름 바꾸는 방법
- git remote add origin https://github.com/qresfcgrrddre/github_ttest.git
 : 등록하는 방법
- git remote get-url origin: 연결된 주소 확인
- git push -u origin main: 작업한 파일 깃허브에 올리기
- git clone 주소: 깃허브에 연결 & 내용 복사해서 그대로  받아옴